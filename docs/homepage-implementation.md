# Homepage implementation

The homepage uses the shared frontend layout and Payload Header/Footer globals.
It is served at `/`; the previous `/homepage-preview` URL redirects there.

See [Client preview on dev-site](branch-preview.md) for the current page structure,
editable blocks, separate CMS environment setup, and client review workflow.
