// The Home document uses the shared CMS renderer and the global site layout.
// This branch replaces the legacy holding-page output without modifying CMS data.
export { default, generateMetadata } from './[slug]/page'
