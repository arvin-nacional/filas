// The published Home document stays live while Payload serves draft previews
// through the same renderer. New installations use the FILAS homepage defaults.
export { default, generateMetadata } from './[slug]/page'
